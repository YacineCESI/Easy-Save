<Window x:Class="EasySaveV2.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EasySaveV2"
        mc:Ignorable="d"
        Title="EasySave" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="160"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Panneau de navigation à gauche -->
        <StackPanel Grid.Column="0" Background="White">
            <Button Content="Create" Height="40" Background="Black" Foreground="White" Margin="10" HorizontalAlignment="Stretch" Click="CreateButton_Click"/>
            <Button Content="Settings" Height="40" Background="Black" Foreground="White" Margin="10" HorizontalAlignment="Stretch" Click="Button_Click_1"/>
        </StackPanel>

        <!-- Contenu principal -->
        <Grid Grid.Column="1" Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Barre de progression -->
            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <ProgressBar Grid.Column="0" Height="20" Grid.ColumnSpan="2" />
            </Grid>

            <!-- Boutons de contrôle -->
            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,15,0,15">
                <Button Content="Resume" Width="80" Height="30" Background="Black" Foreground="White" Margin="0,0,10,0" Click="ResumeButton_Click"/>
                <Button Content="Pause" Width="80" Height="30" Background="Black" Foreground="White" Margin="0,0,10,0" Click="PauseButton_Click"/>
                <Button Content="Remove" Width="80" Height="30" Background="Black" Foreground="White" Margin="0,0,10,0" Click="RemoveButton_Click"/>
                <Button Content="Run all" Width="80" Height="30" Background="Black" Foreground="White" Margin="0,0,10,0" Click="RunAllButton_Click"/>
            </StackPanel>

            <!-- Liste des jobs -->
            <ListView Grid.Row="2" Name="JobsListView" SelectionMode="Single">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Nom" Width="100" DisplayMemberBinding="{Binding Name}"/>
                        <GridViewColumn Header="Type" Width="100" DisplayMemberBinding="{Binding Type}"/>
                        <GridViewColumn Header="Source" Width="100" DisplayMemberBinding="{Binding SourceDirectory}"/>
                        <GridViewColumn Header="Destination" Width="100" DisplayMemberBinding="{Binding TargetDirectory}"/>
                        <GridViewColumn Header="Actions" Width="150">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <Button Content="Run" Width="50" Margin="2" Click="RunButton_Click"/>
                                        <Button Content="Stop" Width="50" Margin="2" Click="StopButton_Click"/>
                                    </StackPanel>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                    </GridView>
                </ListView.View>
            </ListView>
        </Grid>
    </Grid>
</Window>